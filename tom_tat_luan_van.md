# Tóm tắt luận văn

**Đề tài:** Attention-based Token Merging Aspect Based Sentiment Analysis

## 1. Đặt vấn đề

Cùng với sự phát triển của các nền tảng đặt phòng trực tuyến và mạng xã hội du lịch, số lượng đánh giá (review) của khách hàng dành cho các cơ sở lưu trú tăng trưởng nhanh chóng cả về số lượng và mức độ chi tiết. Một đánh giá duy nhất thường đề cập đồng thời đến nhiều khía cạnh khác nhau của dịch vụ – ví dụ một câu như "The room was beautiful but the staff was rude" thể hiện cảm xúc tích cực đối với khía cạnh phòng (FACILITY) nhưng tiêu cực đối với khía cạnh nhân viên (SERVICE). Các hệ thống phân tích cảm xúc truyền thống chỉ gán một nhãn cảm xúc duy nhất cho toàn bộ câu/đoạn văn, do đó không phản ánh được những mâu thuẫn cảm xúc cục bộ như trên và khiến doanh nghiệp khó xác định chính xác điểm mạnh, điểm yếu trong vận hành.

Phân tích cảm xúc theo khía cạnh (Aspect-Based Sentiment Analysis – ABSA) ra đời nhằm giải quyết hạn chế này, với hai bài toán con phổ biến: (1) Aspect Term Extraction (ATE) – trích xuất cụm từ thể hiện khía cạnh trong câu, và (2) Aspect (Category) Polarity Classification (APC) – phân loại cảm xúc (tích cực/tiêu cực/trung lập) và nhóm khía cạnh (vd. SERVICE, FACILITY, AMENITY, EXPERIENCE, BRANDING, LOYALTY) gắn với từng cụm từ đó. Sự xuất hiện của các mô hình ngôn ngữ tiền huấn luyện dựa trên Transformer như BERT [2] đã cải thiện đáng kể độ chính xác của ABSA. Đặc biệt, kiến trúc FAST-LCF-BERT kết hợp cơ chế Local Context Focus (LCF) [9], được xây dựng trên nền tảng nghiên cứu khai thác BERT cho ABSA của Li và cộng sự [5], cho phép mô hình tập trung biểu diễn vào vùng ngữ cảnh xung quanh khía cạnh đang xét, từ đó nâng cao độ chính xác phân loại cảm xúc.

## 2. Trích xuất khía cạnh (ATE) trong đề tài

Trong hai bài toán con nói trên, bài toán ATE trong đề tài này không được giải quyết theo lối gán nhãn chuỗi truyền thống (sequence labeling theo sơ đồ BIO), mà được hình thức hoá thành một bài toán **sinh có điều kiện (conditional generation)** dựa trên mô hình ngôn ngữ T5. Cụ thể, mô hình `T5AspectExtractor` — xây dựng trên `T5ForConditionalGeneration` (backbone `t5-base`) — nhận đầu vào là câu đánh giá và sinh trực tiếp danh sách các cụm từ khía cạnh dưới dạng chuỗi có cấu trúc, theo định dạng GAS (Generative Aspect-based Sentiment), ví dụ: `"(phòng); (nhân viên); (thang máy)"`; trường hợp câu không chứa khía cạnh nào, mô hình sinh ra chuỗi `"none"`. Cách tiếp cận sinh này giúp loại bỏ nhu cầu thiết kế nhãn token thủ công và xử lý linh hoạt các câu chứa nhiều khía cạnh, nhưng đồng thời phát sinh rủi ro mô hình sinh ra cụm từ lệch so với văn bản gốc (lỗi chính tả, thiếu/thừa từ). Để khắc phục, đầu ra của T5 được chuẩn hoá bằng kỹ thuật **đối sánh n-gram theo khoảng cách Levenshtein**: mỗi cụm từ được sinh ra sẽ được ánh xạ về n-gram gần nhất xuất hiện thực sự trong câu, bảo đảm các cụm từ khía cạnh trích xuất luôn là chuỗi con hợp lệ của câu đầu vào.

Trên cơ sở module ATE này, đề tài khảo sát hai kiến trúc tổ chức bài toán ABSA đầu-cuối (end-to-end), trích xuất bộ ba (cụm từ khía cạnh, nhóm khía cạnh, cảm xúc): (i) kiến trúc **Joint** — ATE và APC được huấn luyện và đánh giá đồng thời, nhờ đó tránh được hiện tượng lan truyền lỗi giữa hai bước; và (ii) kiến trúc **Pipeline** — T5 thực hiện ATE trước, sau đó các cụm từ trích xuất được đưa sang module APC (FAST-LCF-BERT) để phân loại nhóm khía cạnh và cảm xúc một cách độc lập. Việc tách rời theo dạng pipeline mang lại tính linh hoạt (có thể thay thế từng thành phần), song kết quả thực nghiệm cho thấy độ chính xác trích xuất khía cạnh suy giảm rõ rệt so với kiến trúc Joint (ATE F1 khoảng 60% ở Pipeline so với 79,87% ở Joint), phản ánh chi phí của hiện tượng lan truyền lỗi khi ATE bị tách khỏi APC.

## 3. Bài toán hiệu năng tính toán

Tuy nhiên, lợi ích về độ chính xác của các mô hình dựa trên Transformer đi kèm với chi phí tính toán đáng kể: cơ chế tự chú ý (self-attention) có độ phức tạp O(L²) theo độ dài chuỗi token L. Trong bối cảnh triển khai thực tế – chẳng hạn một trung tâm chăm sóc khách hàng (Customer Care Center) cần xử lý số lượng lớn phản hồi mỗi ngày theo thời gian gần thực – chi phí này trở thành rào cản, đặc biệt khi mô hình được huấn luyện đa nhiệm (đồng thời dự đoán cảm xúc và nhóm khía cạnh) trên các câu dài, nhiều thông tin dư thừa (stopword, dấu câu, các token lặp về ngữ nghĩa).

Một hướng tiếp cận gần đây để giảm chi phí suy luận của Transformer là Token Merging (ToMe) [1], ban đầu được đề xuất cho Vision Transformer: các token có biểu diễn (embedding) tương tự nhau – đo bằng độ tương đồng cosine – được gộp lại thành một token đại diện, giúp giảm độ dài chuỗi qua mỗi lớp mà không cần huấn luyện lại từ đầu. Ý tưởng này mở ra khả năng giảm độ dài chuỗi đầu vào cho các lớp xử lý phía sau trong kiến trúc FAST-LCF-BERT. Tuy nhiên, việc áp dụng trực tiếp chiến lược ghép cặp (bipartite matching) của ToMe gốc cho bài toán ABSA tiềm ẩn rủi ro: nếu các token thuộc cụm từ khía cạnh (aspect term) – vốn được đánh dấu bởi vector LCF – bị gộp một cách "vô tư" theo độ tương đồng embedding mà không xét đến vai trò ngữ nghĩa của chúng, thông tin cục bộ quan trọng nhất cho việc phân loại cảm xúc có thể bị suy giảm.

## 4. Động lực và mục tiêu nghiên cứu

Từ những phân tích trên, có thể thấy còn thiếu các nghiên cứu so sánh một cách hệ thống giữa nhiều chiến lược gộp token khác nhau (không chỉ ghép cặp bipartite toàn cục, mà còn các chiến lược dựa trên lân cận tuần tự hoặc dựa trên trọng số chú ý) khi kết hợp với cơ chế LCF, trong một mô hình ABSA đa nhiệm (đồng thời dự đoán cảm xúc và nhóm khía cạnh). Đây chính là động lực để đề tài "Attention-based Token Merging Aspect Based Sentiment Analysis" được thực hiện: xây dựng và đánh giá thực nghiệm các chiến lược gộp token mới, đặt trong sự tương tác với cơ chế LCF, trên một bộ dữ liệu đánh giá khách sạn thực tế, nhằm tìm ra điểm cân bằng hợp lý giữa hiệu năng tính toán và độ chính xác.
